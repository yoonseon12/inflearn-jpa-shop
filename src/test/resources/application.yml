spring:
  config.activate.on-profile: test
#  
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password: 
#    driver-class-name: org.h2.Driver
#    
#  jpa:
#    hibernate:
#      ddl-auto: create-drop # create : 애플리케이션 실행 시점에 엔티티정보를 drop후 create하고 애플리케이션을 실행.
#                            # create-drop : 애플리케이션 종료 시점에 엔티티 정보를 drop.
#    properties:
#      hibernate:
  #        show-sql: true
#        format_sql: true
       
logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace